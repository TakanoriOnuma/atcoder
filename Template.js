/**
 * @see
 */

/**
 * メイン実行
 * @param {string} input - 入力
 */
function Main(input) {
  const inputList = input.split("\n");

  // 回答
  console.log();
}

Main(require("fs").readFileSync("/dev/stdin", "utf8"));
